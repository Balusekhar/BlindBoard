pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        maven {
            url = "https://maven.fleksy.com/maven"
        }
        mavenCentral()
    }
}

rootProject.name = "Keyboard by Neat Roots"
include ':app'
